# Shelf builds

Unofficial builds of Shelf. Each release carries the installable zip, the update bundle and `latest.json`, which the app reads for updates. Not notarised: after unzipping, run `xattr -dr com.apple.quarantine Shelf.app` once.
